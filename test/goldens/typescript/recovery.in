const single = 'unterminated
const afterSingle: number = 1;
const double = "unterminated
const afterDouble: boolean = true;
const escaped = "tab:\t unicode:\u0041 hex:\x41";
const dollars = `Price: $5 and ${afterSingle}`;
const pattern: RegExp = /a\/b+/dgiu;
const binary = 0b1010_0101;
const bigintValue = 123_456n;

/* Unterminated comments remain comments through end of input.
still a comment
